{
  "name": "smoothcapture-docs",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.8",
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build",
    "preview": "astro preview",
    "deploy": "bun run build && wrangler deploy",
    "deploy:preview": "bun run build && wrangler dev"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.8",
    "@astrojs/starlight": "^0.38.4",
    "astro": "^6.1.9",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "wrangler": "^4.85.0"
  }
}
